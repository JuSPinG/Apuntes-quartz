---
aliases:
  - Práctica de Bind9
---
Enlace: [Exploramos el servicio DNS](https://lasalleinstitucion.sallenet.org/mod/book/view.php?id=363616&chapterid=2418).

# Pasos

Comprueba el estado del servicio DNS utilizando el comando sobre consola correspondiente.

```sh
$ sudo systemctl status bind9
● named.service - BIND Domain Name Server
     Loaded: loaded (/lib/systemd/system/named.service; enabled; preset: enabled)
     Active: active (running) since Mon 2025-09-29 08:36:08 CEST; 46min ago
       Docs: man:named(8)
   Main PID: 474 (named)
     Status: "running"
      Tasks: 5 (limit: 1086)
     Memory: 10.6M
        CPU: 312ms
     CGroup: /system.slice/named.service
             └─474 /usr/sbin/named -f -u bind

sep 29 08:42:01 alumno named[474]: network unreachable resolving './NS/IN': 2001:500:1::53#53
sep 29 08:42:01 alumno named[474]: network unreachable resolving './NS/IN': 2001:500:2f::f#53
sep 29 08:42:03 alumno named[474]: shut down hung fetch while resolving 'www.google.com/A'
sep 29 08:42:04 alumno named[474]: network unreachable resolving './NS/IN': 2001:500:12::d0d#53
sep 29 08:42:04 alumno named[474]: network unreachable resolving './NS/IN': 2001:7fd::1#53
sep 29 08:42:04 alumno named[474]: network unreachable resolving './NS/IN': 2001:500:2::c#53
sep 29 08:42:09 alumno named[474]: network unreachable resolving './NS/IN': 2001:500:a8::e#53
sep 29 08:42:09 alumno named[474]: network unreachable resolving './NS/IN': 2001:500:9f::42#53
sep 29 08:42:09 alumno named[474]: network unreachable resolving './NS/IN': 2001:7fe::53#53
sep 29 08:42:11 alumno named[474]: resolver priming query complete: timed out
```

Con el servicio arrancado, interroga a tu servidor, utilizando el comando _host_, por ejemplo `host -a www.google.com 127.0.0.1` (El 127.0.0.1 indica quien resuelve el nombre). ¿Hay alguna diferencia entre hacerlo desde el colegio o hacerlo desde casa?

```sh
$ host -a www.google.com 127.0.0.1
Trying "www.google.com"
;; communications error to 127.0.0.1#53: timed out
;; communications error to 127.0.0.1#53: timed out
;; no servers could be reached
```

1. Crea una zona de resolución directa maestra denominada "asirXX.asir", donde XX es tu número de lista.
	1. El nombre de la zona debe ser el del dominio "asirXX.asir" y el del archivo que la contiene "asirXX.asir.hosts" (ten cuidado con los permisos del fichero).
	2. El SOA deberá tener como valores: 
		1. Correo electrónico: uno con sentido (sustituyendo la "@" por un ".")
		2. Número de serie acorde con el formato `AAAAMMDDNN`.
		3. Refresco de la zona cada hora (en segundos)
		4. Reintento en caso de no poder transferir la zona cada 10 minutos (en segundos)
		5. ¿Cuándo debe de ofrecer autoridad sobre los registros? Pasadas 2 semanas (en segundos)
		6. TTL por defecto de 3600 segundos
    3. El Servidor Maestro es el propio servidor DNS BOOKWORMXXB, por lo que habrá que crear el registro NS correspondiente.

```sh
$ cat db.asi21.asir.hosts

...
;
; BIND reverse data file for "this host on this network" zone
;
$TTL	604800
@	IN	SOA	localhost. mspineiro.asir21.asir. (
		   20250929		; Serial
			 604800		; Refresh
			    600		; Retry
			  12096		; Expire
			   3600 )	; Negative Cache TTL
;
@       IN      NS      bookworm21b.asir21.asir.
```

```sh
$ cat named.conf.default-zones 

...
zone "asir21.asir" {
	type master;
	file "/etc/bind/db.asir21.asir.hosts";
};
```

```sh
$ sudo named-checkzone asir21.asir /etc/bind/db.asir21.asir.hosts

zone asir21.asir/IN: loaded serial 20250929
OK

$ sudo systemctl restart bind9
```

2. Ve introduciendo los siguientes registros de uno en uno. Según los introduzcas recarga el servicio para hacer las correspondientes comprobaciones:
	1. Registro NS que apunte a tu máquina BOOKWORMXXB, pues será él mismo el servidor DNS y propietario de la zona que estamos creando.
	2. Registro A para tu máquina con la dirección correspondiente como servidor según venimos utilizando: IP 10.0.128+X.2 para bookwormXXb.asirXX.asir. Se cuidadoso con el nombre. Si lo cualificas acábalo en punto, si no lo cualificas no pongas el punto al final.
	3. Registro A para 'bookwormXX' a la dirección correspondiente a tu red.
	4. Registro A para una máquina denominada 'servidor' en la dirección IP 10.0.128+XX.200 de tu red.
	5. Registro AAAA para una máquina denominada 'servidor' en la dirección IP fe80:XX:XX::XX+200 /48.
	6. Registro CNAME para www que se traduzca por el nombre de tu máquina "bookwormXXb".
	7. Registro MX para 'correo.asirXX.asir' que se traduzca por el nombre de tu máquina con prioridad 10.

```sh
$ cat nano db.asir21.asir.hosts 

...
bookworm21b.asir21.asir.    IN A    10.0.149.2
bookworm21a                 IN A    10.0.149.1
servidor                    IN A    10.0.149.200
servidor                    IN AAAA fe80:21:21::221
www                         IN CNAME bookworm21b.asir21.asir.
correo                      IN MX   10 bookworm21b.asir21.asir.
```

3. Haz pruebas desde tu servidor para comprobar si las interrogaciones funcionan correctamente (recuerda la Práctica 0). Interroga específicamente sobre los tipos de registro, por ejemplo: `host -t CNAME www.asirXX.asir. 127.0.0.1`.

```sh
$ host -t CNAME www.asir21.asir. 127.0.0.1

Using domain server:
Name: 127.0.0.1
Address: 127.0.0.1#53
Aliases: 

www.asir21.asir is an alias for bookworm21b.asir21.asir.
```

4. Haz pruebas desde tu máquina cliente LINUXXX para comprobar si las interrogaciones funcionan correctamente (recuerda la Práctica 0). Para ello:
    1. Modifica la configuración de red del cliente LINUXXX para que el DNS sea el 10.0.128+XX.2
    2. Interroga con comandos análogos a los del apartado 5 pero sin poner el 127.0.0.1 del final para que sea el valor de DNS configurado el que determine quién resuelve los nombres.

```sh
$ host -t CNAME www.asir21.asir. 10.0.149.2

Using domain server:
Name: 10.0.149.2
Address: 10.0.149.2#53
Aliases: 

www.asir21.asir is an alias for bookworm21b.asir21.asir.
```

5. Crea un subdominio (subzona, sin delegación) denominado "subasirXX.asirXX.asir" dentro de la propia zona creada en la práctica anterior. Utiliza para ello la directiva `$origin`
6. Crea los siguientes registros adicionales debajo de la directiva $origin (Puedes seguir el ejemplo que se encuentra en el "howto" del apartado 9 de Zytrax al respecto de subdominios):
	1. servidor1 debe resolverse como un alias de servidor.asirXX.asir (es decir servidor1.subasirXX.asirXX.asir es un alias de servidor.asirXX.asir).
	2. ftp debe resolverse con la IP 10.0.128+XX.2.

```sh
$ cat db.asir21.asir.hosts

...
$origin subasir21.asir21.asir

servidor1			IN  CNAME	servidor.asir21.asir.
ftp 				IN      A	10.0.149.2
```

7. Recarga el servicio y haz pruebas para comprobar que se resuelven los nombres tanto del dominio como del subdominio correctamente.
8. Realiza consultas DNS desde el cliente haciendo capturas de ello como en prácticas anteriores.

```sh
$ host -a ftp.subasir21.asir21.asir. 10.0.149.2
Trying "ftp.subasir21.asir21.asir"
Using domain server:
Name: 10.0.149.2
Address: 10.0.149.2#53
Aliases: 

;; ->>HEADER<<- opcode: QUERY, status: NOERROR, id: 62822
;; flags: qr aa rd ra; QUERY: 1, ANSWER: 1, AUTHORITY: 0, ADDITIONAL: 0

;; QUESTION SECTION:
;ftp.subasir21.asir21.asir.     IN      ANY

;; ANSWER SECTION:
ftp.subasir21.asir21.asir. 604800 IN    A       10.0.149.2

Received 59 bytes from 10.0.149.2#53 in 2 ms
```

9. Crea una zona MAESTRA de resolución inversa con los registros PTR correspondientes a las IPs que hemos tratado en las anteriores prácticas.

```sh
$ cat named.conf.local 
//
// Do any local configuration here
//

// Consider adding the 1918 zones here, if they are not used in your
// organization
//include "/etc/bind/zones.rfc1918";

zone "149.0.10.in-addr.arpa.rev" {
    type master;
    file "/etc/bind/db.149.0.10.in-addr.arpa";
};
```

10. El nombre de la zona será "128+XX.0.10.in-addr.arpa.rev". Será también una zona maestra con los mismos registros SOA y NS que la de las prácticas anteriores (excepto el número de serie en el caso del SOA). Recuerda que aquello que no cualifiques será cualificado con "128+XX.0.10.in-addr.arpa".

11. Los registros adicionales que deben crearse son PTR.

```sh
$ cat db.149.0.10.in-addr.arpa.rev

$TTL 604800
@    IN SOA  localhost. mspineiro.asir21.asir. (
            20251001 ; Serial (aumenta cuando modifiques)
            604800   ; Refresh
            600      ; Retry
            1209600  ; Expire
            3600 )   ; Negative Cache TTL

@    IN NS bookworm21b.asir21.asir.

$ORIGIN 149.0.10.in-addr.arpa.

2       IN PTR bookworm21b.asir21.asir.
1       IN PTR bookworm21.asir21.asir.
200     IN PTR servidor.asir21.asir.
```

12. ¿Cómo sería la consulta con dig para resolver la IP 10.0.128+XX+200? Prueba que funciona.

```sh
$ dig -x 10.0.149.200 @10.0.149.2

; <<>> DiG 9.18.33-1~deb12u2-Debian <<>> -x 10.0.149.200 @10.0.149.2
;; global options: +cmd
;; Got answer:
;; ->>HEADER<<- opcode: QUERY, status: NXDOMAIN, id: 43646
;; flags: qr aa rd ra; QUERY: 1, ANSWER: 0, AUTHORITY: 1, ADDITIONAL: 1

;; OPT PSEUDOSECTION:
; EDNS: version: 0, flags:; udp: 1232
; COOKIE: 58ec25b26057467c0100000068dd6ea9907a35463a846798 (good)
;; QUESTION SECTION:
;200.149.0.10.in-addr.arpa.	IN	PTR

;; AUTHORITY SECTION:
10.IN-ADDR.ARPA.	86400	IN	SOA	10.IN-ADDR.ARPA. . 0 28800 7200 604800 86400

;; Query time: 0 msec
;; SERVER: 10.0.149.2#53(10.0.149.2) (UDP)
;; WHEN: Wed Oct 01 20:10:49 CEST 2025
;; MSG SIZE  rcvd: 132
```

13. El nombre de la zona y por tanto el sufijo indica el rango de red. ¿Cómo sería si en lugar de /24 fuera /26?

14. En la zona "asirXX.asir" crea un nuevo un subdominio denominado "delegadoasirXX.asirXX.asir" del mismo modo que en la práctica anterior. Utiliza para ello la directiva `$origin`.

```sh
$ cat db.asir21.asir.hosts

...
$ORIGIN delegadosasir21.asir22.asir.

@				IN         NS       servidor.asirXX.asir.
```

15. Asigna a ese subdominio como registro NS el DNS servidor.asirXX.asir (se trata de un DNS ficticio, que no existe) y vuelve a cargar la zona. Para ello tendrás que habilitar un registro NS específico para "delegadoasirXX.asirX.asir". Para ello, después de la directiva $origin, usa la sintaxis: 
	1. ``@            IN         NS       servidor.asirXX.asir``.
	2. No añadimos ningún registro más.

16. Vamos a consultar desde nuestro cliente Linux el nombre "prueba.delegadoasirXX.asirXX.asir" usando el comando dig. Si no indicamos nada de forma expresa, la pregunta **recursiva**. Observa qué ocurre. Observa la respuesta que se da. Observa la captura de paquetes que se produce en la interfaz de la red interna del cliente LinuxXX y extrae conclusiones. Recuerda por filtrar por DNS para que sea más sencillo.

```sh
$ dig delegadoasir21.asir21.asir @10.0.149.2

; <<>> DiG 9.18.30-0ubuntu0.24.04.2-Ubuntu <<>> delegadoasir21.asir21.asir @10.0.149.2
;; global options: +cmd
;; Got answer:
;; ->>HEADER<<- opcode: QUERY, status: NXDOMAIN, id: 4006
;; flags: qr aa rd ra; QUERY: 1, ANSWER: 0, AUTHORITY: 1, ADDITIONAL: 1

;; OPT PSEUDOSECTION:
; EDNS: version: 0, flags:; udp: 1232
; COOKIE: 81020d6c52e6720b0100000068e36c742348ff41b655e0ad (good)
;; QUESTION SECTION:
;delegadoasir21.asir21.asir.    IN      A

;; AUTHORITY SECTION:
asir21.asir.            3600    IN      SOA     localhost. mspineiro.asir21.asir. 20250930 604800 600 1209600 3600

;; Query time: 6 msec
;; SERVER: 10.0.149.2#53(10.0.149.2) (UDP)
;; WHEN: Mon Oct 06 09:15:00 CEST 2025
;; MSG SIZE  rcvd: 138
```

17. Pregunta por "prueba.delegadoasirX.asirXX.asir" usando el comando dig, pero haciendo que la pregunta sea **NO recursiva**. Observa qué ocurre y la respuesta que se da. Observa la captura de paquetes que se produce en la interfaz de la red interna del cliente LinuxXX y extrae conclusiones. Recuerda por filtrar por DNS para que sea más sencillo.

```sh
$ dig prueba.delegadoasir21.asir21.asir +norecurse @10.0.149.2

; <<>> DiG 9.18.30-0ubuntu0.24.04.2-Ubuntu <<>> prueba.delegadoasir21.asir21.asir +norecurse @10.0.149.2
;; global options: +cmd
;; Got answer:
;; ->>HEADER<<- opcode: QUERY, status: NXDOMAIN, id: 54254
;; flags: qr aa ra; QUERY: 1, ANSWER: 0, AUTHORITY: 1, ADDITIONAL: 1

;; OPT PSEUDOSECTION:
; EDNS: version: 0, flags:; udp: 1232
; COOKIE: 435d268f9b1a55360100000068e36c97a967d4fda0013847 (good)
;; QUESTION SECTION:
;prueba.delegadoasir21.asir21.asir. IN  A

;; AUTHORITY SECTION:
asir21.asir.            3600    IN      SOA     localhost. mspineiro.asir21.asir. 20250930 604800 600 1209600 3600

;; Query time: 2 msec
;; SERVER: 10.0.149.2#53(10.0.149.2) (UDP)
;; WHEN: Mon Oct 06 09:15:34 CEST 2025
;; MSG SIZE  rcvd: 156
```

18. A partir de la configuración de la práctica anterior, se pueden configurar reenviadores en el servidor "B" bien generales, bien para un dominio o dominios específicos.
19. La configuración para clase será: un reenviador general a la dirección 172.16.5.65 y un reenviador condicional para el dominio google.com al servidor 172.16.5.67. La configuración para casa será un reenviador general a la dirección 1.1.1.1 y un reenviador condicional para el dominio google.com al servidor 8.8.8.8.
```sh
$ cat named.conf.local

...
zone "google.com" {
    type forward;
    forwarders { 
        172.16.5.67; 
    };
};

$ cat named.conf.options

options {
	directory "/var/cache/bind";

	 forwarders {
		172.16.5.65;
	};

	dnssec-validation no;
	listen-on-v6 { none; };
};
```
18. Si no tenemos ningún reenviador configurado el servidor DNS instalado en nuestro servidor B buscará hacer consultas recursivas para obtener respuestas, cachearlas y poder ofrecerlas al cliente WindowsXX. Si hacemos la configuración correspondiente que aparece en el punto 17 y optamos por la opción forward only nuestro servidor DNS dejará de usar la recursividad y reenviará las consultas a los servidores DNS configurados como reenviadores.
19. Crea una nueva zona maestra de resolución directa en 'BOOKWORMXXA' denominada "otroasirXX.asir" con las siguientes características y haz las correspondientes pruebas que verifiquen su correcto funcionamiento.
    1. SOA: bookwormXXa.otroasirXX.asir y parámetros de tiempo como en prácticas anteriores, salvo los indicados en las siguientes líneas
    2. NS: bookwormXXa.otroasirXX.asir.
    3. A: 10.0.128+XX.1 es la IP de bookwormXXa.otroasirXX.asir.
    4. CNAME: www es alias de bookwormXXa.otroasirXX.asir.
    5. Servidor2 tiene como IP 10.0.128+XX.254.
    6. El tiempo de refresco de la zona será de 180 segundos y el de reintento será de 30 segundos.
	    ```sh
		$ cat db.otroasir21.asir
			
		$TTL	604800
		@	IN	SOA	bookworm21a.otroasir21.asir. mspineiro.otroasir21.asir. (
					      1		; Serial
					    180		; Refresh
					     30		; Retry
					2419200		; Expire
					 604800 )	; Negative Cache TTL
		;
		@				IN	NS	bookworm21a.otroasir21.asir.
		bookworm21a.otroasir21.asir.	IN	 A	10.0.149.1
		www				IN   CNAME	bookworm21a.otroasir21.asir.
		servidor2		IN	 A	10.0.149.2
		```
20. Pon a capturar el tráfico en uno de los servidores
    ![[Tráfico entre A y B por otroasir21.asir.png]]
21. Crea una nueva zona "esclava" de resolución directa en BOOKWORMXXB denominada "otroasirXX.asir" (la zona del servidor maestro y del esclavo tienen que llamarse igual. El servidor maestro será 'bookwormXXa'. Se indicará con la correspondiente IP. Al recargar la configuración debería producirse una solicitud de SOA y la correspondiente transferencia de zona. Observa el tráfico que se produce y pregunta al servidor B por 'servidor2.otroasirXX.asir' para constatar que funciona.
    ```sh
    $ cat named.conf.local # Desde el servidor bookworm21b
     
	...
    zone "otroasir21.asir" {
		type slave;
		file "/var/cache/bind/db.otroasir21.asir.slave";
		masters { 10.0.149.1; };
	};
	
	$ cat named.conf.local # Desde el servidor bookworm21a
	
	...	
	zone "otroasir21.asir" {
		type master;
		file "/etc/bind/db.otroasir21.asir";
		allow-transfer { 10.0.149.2; };
	};
    ```
    ![[Tráfico entre A y B por servidor2.otroasir21.asir.png]]
22. Observa como cada cierto tiempo se produce la consulta del SOA por parte del esclavo pero no hay transferencia de zona.
23. Modifica la zona maestra cambiando la IP 10.0.128+XX.254 por la 253, cambia el número de serie y recarga la zona. Observa si al recargar la zona se produce una notificación inmediata, una solicitud de SOA y la transferencia IXFR que permita actualizar la IP que ha cambiado.