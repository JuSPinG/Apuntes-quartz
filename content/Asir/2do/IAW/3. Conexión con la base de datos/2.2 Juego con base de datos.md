---
aliases:
  - Juego con base de datos
  - DB_GAMES_PHP
---
# Creando la base de datos

Lo primero de todo, deberemos crear una base de datos, pues de eso mismo va el juego. Lo haremos de acuerdo a la siguiente imagen.

![[Esquema de la base de datos.png]]

```sql
CREATE gestion_juegos;
USE gestion_juegos;
```


```sql
-- Tabla GAMES
CREATE TABLE GAMES (
	ID INT AUTO_INCREMENT PRIMARY KEY,
	Titulo VARCHAR(100) NOT NULL,
);

-- Tabla PLAYERS
CREATE TABLE PLAYERS (
	ID INT AUTO_INCREMENT PRIMARY KEY,
	Alias VARCHAR(50) NOT NULL,
);

-- Tabla PARTIDAS
CREATE TABLE PARTIDAS (
	ID INT AUTO_INCREMENT PRIMARY KEY,
	Nombre VARCHAR(30),
	Descripción VARCHAR(20),
	ID_GAME INT,
	ID_PLAYER1 INT,
	ID_PLAYER2 INT,
	FOREIGN KEY (ID_GAME) REFERENCES GAMES(ID) ON DELETE CASCADE,
	FOREIGN KEY (ID_PLAYER1) REFERENCES PLAYERS(ID) ON DELETE CASCADE,
	FOREIGN KEY (ID_PLAYER2) REFERENCES PLAYERS(ID) ON DELETE CASCADE
);
```

```sql
-- Datos de prueba
INSERT INTO GAMES (Titulo, Plataforma) VALUES ('League of Legends', 'PC'), ('FIFA 24', 'PS5'), ('Valorant', 'PC');
INSERT INTO PLAYERS (Nick, Nivel) VALUES ('Faker', 99), ('Ibai', 10), ('Rubius', 25), ('Vegetta', 50);
INSERT INTO PARTIDAS (ID_GAME, ID_PLAYER1, ID_PLAYER2, Resultado) VALUES 
(1, 1, 2, '1-0'), (1, 3, 4, '0-1'), (2, 1, 3, '2-2'), (2, 1, 4, '3-1'), (1, 1, 2, 'Remate');
```