---
aliases:
  - Método de la cantidad pivotal para la construcción de intervalos de confianza
---
Enlace: [[Estadística - Modelos Probabilísticos y Optimización.pdf#page=210&selection=82,0,97,23|Estadística - Modelos Probabilísticos y Optimización, página 210]].

Si $T(X_1,X_2,...,X_n;\theta)$ es un estadístico cuya distribución en el muestreo no depende de $\theta$, fijado un valor de $\alpha$, se pueden determinar constantes $c_1$ y $c_2$ tales que:

$$P(c_1\leq T(X_1,X_2,...X_n;\theta)\leq c2)\geq \alpha$$

Y si se puede despejar el estadístico $T$, entonces se puede refactorizar la expresión a:

$$P(t_1(X_1,X_2,...,X_n;c_1)\leq g(\theta)\leq t_2(X_1,X_2,...,X_n;c_2))\geq \alpha$$

Consideramos una población con distribución uniforme en el intervalo $(0, \theta)$, y que sea $X_1,X_2,...,X_n$ una muestra aleatoria de la población. Cada variable $X_i$ tiene una función de densidad igual a:

$$f_\theta(x)=\left\{\begin{matrix}1,&\text{si }0<x<\theta\\0,&\text{en caso contrario}\\\end{matrix}\right.$$

El estadístico:

$$T(X_1,X_2,...,X_n;\theta)=\frac{1}{\theta}\max_{1\leq i\leq n}X_i$$

Tiene una distribución en el muestreo independiente de $\theta$, esto se comprueba porque si $y\in(0,1)$, entonces:

$$P(T(X_1,X_2,...,X_n;\theta)\leq y)=...=y^n$$

Si $y\leq 0$, entonces $P(T(X_1,X_2,...,X_n;\theta)\leq y)=0$, mientras que si $y\geq 1$, $P(T(X_1,X_2,...,X_n;\theta)\leq y)=1$, por lo que el estadístico tiene una distribución igual a $y^n, 0<y<$.

Fijando el nivel de confianza $\alpha$, nos damos cuenta de que $y_1<y_2$ tales que $y_2^n-y^n_1=\alpha$.

No entiendo nada, #Código/Entrada-incompleta 

Siguiente: [[4.2.3 Intervalos de confianza para los parámetros de las distribuciones normales]].