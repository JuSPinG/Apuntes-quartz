Enlace: [[Fundamentos de programación.pdf#page=234&selection=8,0,16,4|Fundamentos de programación, página 234]].

El tipo booleano puede servir de manera muy útil:

```c
int(false) == 0
int(true) == 1

/*=============*/

bool bisiesto;

bisiesto = (anno % 4) == 0; /* Esto guarda si el año es bisiesto o no */

if (biesiesto && (mes > Febrero)) {
  totalDias = totalDias + 1;
}

/*=============*/

bool isalpha(char c);
bool islower(char c);
...
```

([[2.5 Tipos predefinidos#^2467c7|Continuación de los métodos]])

Siguiente: [[9.4 Tipos estructurados]].